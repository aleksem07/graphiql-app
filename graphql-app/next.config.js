/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.licdn.com', 'avatars.githubusercontent.com'], // Add other domains as needed
    },
};

module.exports = nextConfig;
